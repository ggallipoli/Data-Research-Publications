Do-File Order:

gravity1_build.do
gravity2a_flows.do
gravity2b_theta.do
gravity3_regs.do
gravity4_results.do

All tables and figures from the paper are generated by these do-files.

The sub-folders "Built" and "TexFiles" get populated with output from these do-files.

The source datasets (available separately) are "LPD.dta" and "union_occ.dta".
